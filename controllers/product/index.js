const list = require("./list");
const add = require("./add");
const update = require("./update");
const deleteData = require("./delete");
const show = require("./show");
const all = require("./all");

module.exports = { list, add, update, deleteData, show, all };
